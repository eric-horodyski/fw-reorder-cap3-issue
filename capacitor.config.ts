import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'fw-reorder-cap3-issue',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
